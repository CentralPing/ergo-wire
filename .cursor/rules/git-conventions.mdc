---
description: Git conventions for CentralPing repos
alwaysApply: true
---

# Git Conventions

All CentralPing repos enforce signed commits via branch protection. The `-S` flag is **required** on every commit.

```bash
git commit -S -m "your commit message"
```

Use GitHub MCP tools or `gh` CLI for GitHub operations.
